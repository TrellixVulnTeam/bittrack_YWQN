    // $.getJSON("pricex_2006_3006.json", function(json) {
    //     console.log(json[1]); // this will show the info it in firebug console
    // });

    // var old_prices = require('./pricex_2006_3006.json');
    // old_prices = old_prices.map((old_price) => {
    //     return old_price.close;})
    // console.log(utils.calculateRsi(old_prices.slice(5)));

    // const status = await utils.getApiStatus(binance);
    // const balance = await utils.getBalance(binance);

    // console.log('Playing function');
    // console.log(status);
    // console.log('USDC: ' + balance.total.USDC);
    
    // let pricex = fs.readFileSync('pricex_2006_3006.json');
    // pricex = JSON.parse(pricex);
    // console.log(pricex);